services:
  # Database for user accounts, transactions, and history
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: stock_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # Message broker for Celery tasks
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  # Result backend and lock manager for Celery
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  # FastAPI Backend
  api:
    build:
      context: .
      dockerfile: api/Dockerfile
    image: stock-pred-backend:latest
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:password@db:5432/stock_db
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672//
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - ./api:/app
    depends_on:
      - db
      - rabbitmq
      - redis
    ports:
      - "8000:8000"

  celery-beat:
    build:
      context: .
      dockerfile: api/Dockerfile
    image: stock-pred-backend:latest
    command: celery -A api.worker.celery_app beat -l info
    volumes:
      - ./api:/app
    depends_on:
      - rabbitmq
      - redis
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:password@db:5432/stock_db
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672//
      - REDIS_URL=redis://redis:6379/0

  # High-priority worker for payment processing
  worker-payments:
    image: stock-pred-backend:latest
    # Routes to 'payments' queue with higher concurrency
    command: celery -A api.worker.celery_app worker -Q payments -c 8 --loglevel=info
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/stock_db
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672//
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - db
      - rabbitmq
      - redis

  # Resource-heavy worker for ML predictions
  worker-ml:
    image: stock-pred-backend:latest
    # Routes to 'ml' queue with concurrency limited to 2 to manage CPU/GPU usage
    command: celery -A api.worker.celery_app worker -Q ml -c 2 --loglevel=info
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/stock_db
      - RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672//
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - db
      - rabbitmq
      - redis

  # Streamlit Frontend
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    environment:
      - API_URL=http://api:8000
    ports:
      - "8501:8501"
    depends_on:
      - api

volumes:
  postgres_data:
  rabbitmq_data:
  redis_data: